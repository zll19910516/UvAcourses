<%= form_for(@course_programme) do |f| %>
  <% if @course_programme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_programme.errors.count, "error") %> prohibited this course_programme from being saved:</h2>

      <ul>
      <% @course_programme.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_sgid %><br />
    <%= f.text_field :course_sgid %>
  </div>
  <div class="field">
    <%= f.label :programme_id %><br />
    <%= f.number_field :programme_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
