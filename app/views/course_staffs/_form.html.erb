<%= form_for(@course_staff) do |f| %>
  <% if @course_staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_staff.errors.count, "error") %> prohibited this course_staff from being saved:</h2>

      <ul>
      <% @course_staff.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_sgid %><br />
    <%= f.text_field :course_sgid %>
  </div>
  <div class="field">
    <%= f.label :staff_id %><br />
    <%= f.number_field :staff_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
